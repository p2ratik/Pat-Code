openai>=1.0.0
pydantic>=2.0.0
rich>=13.0.0
textual>=0.86.0
click>=8.0.0
keyring>=24.0.0
platformdirs>=4.0.0
tiktoken>=0.7.0
fastmcp>=2.0.0
ddgs>=9.0.0
redis>=5.0.0
tomli>=2.0.0; python_version < "3.11"

faiss-cpu==1.13.2
fastembed==0.8.0
numpy>=1.26,<3
sqlite-utils==3.38






openai>=1.0.0

# Optional vector store client (Phase 5) – can be installed when needed
# qdrant-client>=1.12.0

# Misc utilities (already in stdlib, listed for completeness)

# python-dotenv (if you load .env files)
python-dotenv>=1.0.0

# repo_intel dependencies
pathspec>=0.12.0       # .gitignore-aware file discovery
rank-bm25>=0.2.2       # BM25 search over entities
networkx>=3.0   
